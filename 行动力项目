行动力 客户端需要处理的问题
dak1.考勤的下班自动打卡 android ios;ios增加了 是否下班打卡的配置，默认关闭；
2.android ios 缓存清除不了 localstorage，清理缓存时，需要清除掉，不然有些问题无法解决
3.ios 物品领用 点击添加明细 位置不正确，放到下面去了 使用的添加方法如下 
 $(obj).parent().prev().after(html);
4.现在考勤没加限制，可以给多个人打卡，需要添加 判断，本地保存当天第一个打卡人的标识id，进入打卡页面 判断用户id 和本地id是否一致；已处理：
不允许 给多个用户打卡，可以在服务器配置（考勤组里添加字段 isAllowMutClock 是否允许一个手机打多个人的考勤,默认关闭 0，1 允许）
 如不一致 则给出提示 不让其打卡，并关闭打卡页面；（有个缺陷 清楚缓存后，就没有本地数据了）
5.自动缺卡，根据考勤组里的issection=0，即不支持分段打卡的，获取 考勤组里的 noonTime="13:30" 来进行判断，默认是13:30
6. 文件预览 android ios 需要返回两次，
7.ios 打卡成功后，弹出 EBForeNotification 通知，页面点击就无响应了，可能是，EBForeNotification 弹窗的 空白view导致
