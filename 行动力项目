行动力 客户端需要处理的问题
1.考勤的下班自动打卡 android ios 
2.android ios 缓存清除不了 localstorage，清理缓存时，需要清除掉，不然有些问题无法解决
3.ios 物品领用 点击添加明细 位置不正确，放到下面去了 使用的添加方法如下 
 $(obj).parent().prev().after(html);
4.现在考勤没加限制，可以给多个人打卡，需要添加 判断，本地保存当天第一个打卡人的标识id，进入打卡页面 判断用户id 和本地id是否一致，
 如不一致 则给出提示 不让其打卡，并关闭打卡页面；（有个缺陷 清楚缓存后，就没有本地数据了）
